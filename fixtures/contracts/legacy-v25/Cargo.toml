[package]
name = "kanatoko-legacy-v25-fixture"
version = "0.0.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]
doctest = false

[dependencies]
soroban-sdk = "=25.3.2"

[profile.release]
overflow-checks = true

[lints.rust]
unsafe_code = "forbid"
